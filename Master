#include<stdio.h>
int main()
{
  int i,factorial=1, num;
 
  printf("Enter a natural number: ");
  scanf("%d", &num);
 
  for(i=1;i<=num;i++)
    factorial = f * i;
 
  printf("%d! = %d\n", num, factorial);
  return 0;
  }
